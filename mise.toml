[tools]

python = "3.12.13"
uv = "latest"

"pipx:changes-semver" = "6"

[env]

# Ensure uv uses the Python provisioned by mise above rather than silently downloading an interpreter of its own.
UV_PYTHON_PREFERENCE = "only-system"
